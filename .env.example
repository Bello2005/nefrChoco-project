APP_NAME="IPS NefroChoco"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=America/Bogota
APP_URL=http://localhost

APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_ES

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# Contraseña del admin inicial (admin@nefrochoco.co) que siembra AdminUserSeeder.
# Obligatoria fuera de local/testing: sin ella el seeder falla en vez de crear
# la cuenta con la contraseña de la demo.
ADMIN_INITIAL_PASSWORD=

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=teleproject
DB_USERNAME=postgres
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Jitsi Meet - el servidor autoalojado ya existe (jitsi.bello.works, version fijada
# a stable-11248, branding propio), pero todavia sin autenticacion JWT.
# En produccion esta variable DEBE apuntar al servidor propio, nunca a meet.jit.si:
# ahi cualquiera que averigue el nombre de la sala puede entrar, y ademas la
# conversacion pasa por infraestructura de terceros. El valor de abajo es solo
# el respaldo para desarrollo local.
JITSI_DOMAIN=meet.jit.si

# Consentimiento informado. Al cambiar el texto hay que subir la version:
# quienes aceptaron una anterior vuelven a ver la pantalla correspondiente.
PRIVACY_CONSENT_VERSION=2026-01
PRIVACY_TELECONSULTATION_CONSENT_VERSION=2026-09
PRIVACY_CONTACT_EMAIL=habeasdata@nefrochoco.co

# Dominio de correo exigido al personal (admin y medico) al crear o editar su
# cuenta. Los pacientes no tienen restriccion: usan su correo personal.
# CONFIRMAR con la IPS antes de produccion.
ALLOWED_EMAIL_DOMAIN=nefrochoco.co

# Ventana para entrar a la sala de teleconsulta, en minutos alrededor de la
# hora agendada. Ampliar donde la senal obliga a reconectarse varias veces.
TELECONSULTATION_JOIN_MINUTES_BEFORE=15
TELECONSULTATION_JOIN_MINUTES_AFTER=60

# Institución para el RDA del IHCE. [CONFIRMAR] con la IPS: se dejan vacíos
# hasta tener los datos reales; nunca se inventan.
INSTITUTION_NAME=
INSTITUTION_NIT=
INSTITUTION_REPS_CODE=
INSTITUTION_SITE_CODE=
INSTITUTION_MUNICIPALITY_CODE=
